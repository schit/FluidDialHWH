; Copy to platformio.local.ini (gitignored) for per-machine overrides.
; Useful for stabilizing espota on multi-homed Windows machines.

[env:cyd_resistive_ota]
; Example: force espota to use your PC's WiFi IP + a fixed host port.
; upload_flags =
;     --host_ip=192.168.2.65
;     --host_port=3233
;     --timeout=30
